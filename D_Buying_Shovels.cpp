#include<bits/stdc++.h>
using namespace std;
int main (){

    int t; cin >> t;
    while(t--){
        int n, k; cin >> n >> k;
        int min = INT_MAX;
        if(k >= n) cout << 1 << endl;
        else if(k == 1) cout << n << endl;
        else{
            
            }
    }
    return 0;
}